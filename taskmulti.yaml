version: v1.0.0
steps:
# Build target image
- build: -t {{.Run.Registry}}/hello-worlds:{{.Run.Date}} -f Dockerfile .
# Run image 
- cmd: -t {{.Run.Registry}}/purge:latest
  id: test
# Push image
- push:
  - {{.Run.Registry}}/hello-worlds:{{.Run.Date}}